# ECommerceAutomation

Maven + Selenium + TestNG project scaffold for https://www.saucedemo.com

How to run:
1. Import into Eclipse as existing Maven project or run `mvn test`.
2. Tests are configured in `testng.xml`. Reports are generated at `reports/ExtentReport.html`.
3. Screenshots on failure saved to `screenshots/`.

